import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  boxInput: {
    width: '100%',
    paddingTop: 9,
  },

  input: {
    backgroundColor: "#e9e9e9f2",
    width: "100%",
    height: 60,
    borderRadius: 5,
    paddingLeft: 10,
    gap: 5,
    fontSize: 20,
    paddingRight: 45,
    marginBottom: 5,
  },

  boxIcon: {
    position: 'absolute',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    top: 10,
    right: 10,
    width: 40,
  }
});